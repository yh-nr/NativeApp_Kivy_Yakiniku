#: import FadeTransition kivy.uix.screenmanager.FadeTransition

<AppFrame>:
    screen_manager: sm
    orientation: 'vertical'
    Label:
        id: title_label
        text:"焼き肉画像ランダム表示アプリ"
        font_size: 30
        size_hint_y: 0.1
        canvas.before:
            Color:
                rgba: 0, 0, 0, 1 # 赤色を指定
            Rectangle:
                pos: self.pos
                size: self.size

    MyScreenManager:
        id: sm

    BoxLayout:
        size_hint: 1, 0.2
    
        Button:
            text: "HOME"
            font_size: 30
            on_press:
                title_label.text = "焼き肉写真ランダム表示"
                sm.transition = FadeTransition()
                root.switch2page("HomePage")

        Button:
            text: "犬猫ｶﾒﾗ"
            font_size: 30
            on_press:
                title_label.text = "犬猫分類カメラ"
                sm.transition = FadeTransition()
                root.switch2page("DogCatCam")

        Button:
            text: "アノテーション\nカメラ"
            font_size: 30
            on_press:
                title_label.text = "アノテーション\nカメラ"
                sm.transition = FadeTransition()
                root.switch2page("Cam2annotate")

        Button:
            text: "アノテーション\nカメラ2"
            font_size: 30
            on_press:
                title_label.text = "アノテーション\nカメラ"
                sm.transition = FadeTransition()
                root.switch2page("Cam2annotate2")

